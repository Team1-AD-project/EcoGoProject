# Android UI 特效增强总结

基于 `app (12)` React 应用的完整 UI/UX 还原实现。

## 🎨 视觉风格对齐

### 颜色方案
- **主背景**: `#F0FDF4` (Mint 50)
- **主色调**: `#15803D` (Emerald 700)
- **文本颜色**: `#1E293B` (Slate 800)
- **次要文本**: `#64748B` (Slate 500)
- **边框**: `#E2E8F0` (Slate 200)

### 字体层级
- **标题**: `sans-serif-black`, 20-28sp
- **正文**: `sans-serif-medium`, 13-14sp
- **标签**: 10-12sp
- **统计数字**: `sans-serif-black`, 20sp

## 🎬 动画系统

### 核心动画资源
1. **breathe.xml**: 3 秒呼吸动效（scale 1.0 ↔ 1.02）
   - 应用于：Hero 卡片、头像、地图预览
2. **slide_up.xml**: 300ms 滑入动效（从底部 20% 滑入）
   - 应用于：推荐结果、列表项、Modal 弹窗
3. **pop_in.xml**: 300ms 弹入动效（scale 0.8 → 1.0 + fade）
   - 应用于：推荐卡片、Leader 卡片
4. **jump.xml**: 500ms 跳跃动效（-10% translateY）
   - 应用于：吉祥物点击交互
5. **fade_in.xml**: 300ms 淡入动效
   - 应用于：内容加载
6. **spin.xml**: 1 秒旋转动效（360°）
   - 应用于：加载指示器

## 📱 页面级优化

### Home Fragment (首页)
**布局结构**:
```
Header (问候 + 位置 + 头像)
  ↓
推荐卡片 (动态显示结果 + slideUp 动画)
  ↓
统计卡片网格 (Monthly Points + SoC Score)
  ↓
Hero Card (Next Bus, 深绿渐变 + 10dp 阴影)
  ↓
地图预览 (200dp 高度 + 浮动按钮)
  ↓
Monthly Highlights (水平滚动)
  ↓
Scenic Walks (渐变背景卡片)
```

**关键样式**:
- Hero Card: 135° 渐变 `#15803D → #047857`, elevation 10dp
- 时间标签: 半透明白色背景 `#33FFFFFF`
- 统计卡片: 边框 1dp, fontWeight 800
- 推荐结果: slide_up 动画 + 绿色标签 `#DCFCE7`

### Profile Fragment (个人中心)
**布局结构**:
```
Mascot Header (吉祥物 + 背景装饰)
  ↓
Points Card (深色渐变 `#1E293B → #0F172A`)
  ↓
Tabs (Closet, Badges, History) + 底部指示器
  ↓
RecyclerView (根据 Tab 切换)
```

**关键特效**:
- 吉祥物 ImageView: breathe 动画 + 点击触发 jump
- Points Card: elevation 8dp, 半透明按钮 `#33FFFFFF`
- Tab 指示器: 2dp 高度, 动态显示/隐藏
- Shop Items: 居中布局, 装备状态显示 checkmark

### Routes Fragment (路线)
**卡片设计**:
- **左侧色条**: 4dp 宽，动态颜色（来自 route.color）
- **路线标签**: 彩色背景 + 白色文字, 圆角 8dp
- **状态徽章**: 条件颜色（Arriving: 绿, Delayed: 红, 其他: 蓝）
- **路线图示**: 8dp 圆点 + 箭头 + 起终点

### Community Fragment (社区)
**Leader 卡片**:
- 背景: `#ECFDF5` (浅绿)
- 边框: 1dp solid primary
- emoji 图标: 🏆
- 文本颜色: `#166534`, `#14532D`, `#15803D`

**排行榜项**:
- 排名号: 前 3 名用 primary, 其他用 subtext
- 进度条: 6dp 高度, 圆角 3dp, 动态宽度
- 变化百分比: 正数绿色 `#16A34A`, 负数红色

### Chat Fragment (聊天)
**气泡样式**:
- AI 气泡: 白色背景, 左下角 4dp, 其他 20dp
- 用户气泡: primary 背景, 右下角 4dp, 其他 20dp
- 行间距: 1.5x
- 最大宽度: 280dp
- elevation: 1dp

**输入区域**:
- 输入框: 44dp 高度, 圆角 12dp, 浅灰背景 `#F8FAFC`
- 发送按钮: 44dp 圆形, 999dp 圆角, icon only

### Map Fragment (地图)
**卡片样式**:
- 背景: 浅绿渐变
- Faculty 信息卡: slide_up 动画, 16dp 圆角
- 浮动在底部, 4dp elevation

### Activities Fragment (活动)
**卡片设计**:
- **顶部图像区**: 100dp 高度, 动态颜色
- **分类标签**: 右上角浮动, 半透明白色背景
- **内容区**: 16dp padding, 图标 + 标题 + 日期 + 位置
- **圆角**: 20dp
- **elevation**: 2dp

### Voucher Fragment (兑换券)
**卡片样式**:
- **图标**: 48dp, 动态背景颜色（根据品牌）
- **按钮**: 20dp 圆角, primary 背景
- **不可用状态**: alpha 0.6

## 🎁 组件级优化

### Walking Route Cards
- **渐变背景**: 3 种配色循环
  - 绿色: `#A7F3D0 → #059669` (225°)
  - 蓝色: `#BFDBFE → #3B82F6` (225°)
  - 橙色: `#FDE68A → #D97706` (225°)
- **时间标签**: 半透明白色 `#33FFFFFF`, alpha 0.8
- **尺寸**: 140dp × 160dp

### Shop Items
- **Grid Layout**: 2 列, 6dp margin
- **图标**: emoji, 32sp
- **装备指示器**: checkmark icon, 右上角
- **状态**: Equipped (绿) / Owned (灰) / 价格标签

### Highlights (月度亮点)
- **水平滚动**: 200dp 宽度
- **边框**: 1dp solid border
- **图标**: emoji 20sp
- **描述**: 单行省略, 11sp

### Status Badges
- **条件颜色**:
  - Arriving: `#DCFCE7` 背景, `#15803D` 文字
  - Delayed: `#FEE2E2` 背景, `#B91C1C` 文字
  - Crowded: `#FEF3C7` 背景, `#B45309` 文字
  - 默认: `#E0F2FE` 背景, `#0369A1` 文字
- **样式**: 2-8dp padding, 圆角 8-12dp, fontWeight 700

## 🔄 交互动画

### 点击反馈
- 按钮: ripple effect (`#33FFFFFF`)
- 卡片: 缩放 + 阴影变化
- Tab: 颜色过渡 + 底部指示器滑动

### 加载状态
- 列表: 初始为空, API 返回后淡入
- 推荐: 结果显示时 slide_up
- 聊天: 消息逐条滑入

### 页面过渡
- Fragment 切换: Navigation Component 默认动画
- Modal: slide_up from bottom
- 成功提示: pop_in 弹出

## 📦 资源文件新增

### Drawables
- `bg_hero_gradient.xml`: Hero 卡片渐变
- `bg_route_gradient_[green/blue/orange].xml`: 步行路线渐变
- `bg_points_gradient.xml`: 积分卡渐变
- `bg_recommendation_tag.xml`: 推荐标签背景
- `bg_time_badge.xml`: 半透明白色标签
- `progress_dot[_active].xml`: 进度点

### Icons (Vector Drawables)
- `ic_map_pin`, `ic_sparkles`, `ic_award`, `ic_users`, `ic_leaf`
- `ic_navigation`, `ic_bus`, `ic_message`, `ic_user`
- `ic_settings`, `ic_chevron_right`, `ic_trophy`, `ic_city`, `ic_check`, `ic_send`

### Animations
- 6 个核心动画: breathe, slide_up, pop_in, jump, spin, fade_in, wave

### Styles
- `TextAppearance.EcoGo.*`: 标题、副标题、统计数字、标签
- `CardStyle[.Hero]`: 卡片基础样式和 Hero 变体

## ✅ 一比一还原检查清单

- [x] 颜色方案完全匹配
- [x] 字体大小和 weight 对齐
- [x] 卡片圆角 (12dp, 16dp, 20dp, 24dp)
- [x] 阴影层级 (1dp, 2dp, 4dp, 8dp, 10dp)
- [x] 渐变背景 (Hero, Points, Routes)
- [x] 动画时长和缓动函数对齐
- [x] 半透明元素 (alpha 0.7-0.9)
- [x] 图标尺寸统一 (14-20dp)
- [x] Padding 层级 (12dp, 16dp, 20dp, 24dp)
- [x] Gap spacing (4dp, 6dp, 8dp, 12dp, 16dp)
- [x] 进度条样式 (6dp 高度, 圆角 3dp)
- [x] 状态徽章条件颜色
- [x] Pill shape 按钮 (999dp 圆角)
- [x] 聊天气泡不对称圆角
- [x] Tab 底部指示器 (2dp)

## 🚀 下一步测试建议

1. **启动后端服务**, 确保种子数据已注入
2. **运行 Android 应用**, 检查以下动画:
   - 首页 Hero Card 呼吸效果
   - 推荐结果的滑入动画
   - 吉祥物点击跳跃
   - 列表项的交错淡入
   - Tab 切换的平滑过渡
3. **测试交互流程**:
   - 推荐功能：输入目的地 → 获取推荐
   - 路线浏览：查看彩色条和状态徽章
   - 社区排行榜：Leader 卡片和进度条
   - 聊天界面：发送消息观察气泡样式
   - 个人中心：Tab 切换和底部指示器

## 📝 注意事项

- 所有动画通过 `AnimationUtils.loadAnimation()` 加载
- 渐变背景使用 `layer-list` 或 `gradient` drawable
- 动态颜色通过 `Color.parseColor()` 或 `ContextCompat.getColor()` 设置
- ViewBinding 确保类型安全
- 协程 + LiveData/Flow 用于异步数据加载
- API 失败时回退到 MockData

---
*生成时间: 2026-01-30*
*对齐目标: C:\Users\csls\Desktop\ad-ui\app (12)\index.tsx*
